services:
  simple-multistep:
    image: ghcr.io/chap-models/chapkit_simple_multistep_template:latest
    pull_policy: always
    ports:
      - "8000:8000"
    volumes:
      - chapkit_data:/app/data

volumes:
  chapkit_data:
