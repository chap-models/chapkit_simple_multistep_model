services:
  chapkit-simple-multistep-model:
    image: ghcr.io/chap-models/chapkit_simple_multistep_model:latest
    pull_policy: always
    ports:
      - "8000:8000"
    volumes:
      - chapkit_data:/app/data

volumes:
  chapkit_data:
